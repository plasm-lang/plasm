[package]
name = "mir"
version = "0.1.0"
edition = "2024"

[dependencies]
diagnostic = { path = "../diagnostic" }
hir = { path = "../hir" }
utils = { path = "../utils" }

serde.workspace = true
bimap.workspace = true
