// Warning: This example can't be compiled yet.
// It's written for syntax demonstration.

type Device = enum {
    CPU,
    GPU,
    TPU,
}

// Backing type and discriminant values

@repr(u32)
type HTTPCode = enum {
    @repr(200)
    Success,

    @repr(404)
    NotFound,
}
