fn main() {
    let x = {
        let a = {
            let a = {
                return get_value() + 1 * 2
            }
            return a
        }
        let b = {return (1)}
        return a * b
    }
    let y = {}
    print({
        let value = compute_value(x, 1, 3)
        return value + x
    })
}
