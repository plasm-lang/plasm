fn main() {
    let a = 1 + 2 + 3 * 4 + 5 / 6
    let x = 5
    let y = (x + 10 / 2) * 3 - 1
    let z = {
        let a = x + y
        let b = {
            let a = get_value() + 1 * 2
            return a
        }
        return a * b
    }
    print({
        let value = compute_value(x, y, z)
        return value + 10
    })
}
